<p class="notice"><%= notice %></p>

<div class="aluno-container">
  <%= render @sala %>

  <div class="action-buttons">
    <%= link_to "Editar", edit_sala_path(@sala), class: "btn btn-edit" %>
    <%= link_to "Voltar", salas_path, class: "btn btn-back" %>

    <%= button_to "Excluir", @sala, method: :delete, class: "btn btn-danger" %>
  </div>
</div>
