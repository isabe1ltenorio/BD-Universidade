<%= form_with(model: pre_requisito) do |form| %>
  <% if pre_requisito.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(pre_requisito.errors.count, "error") %> prohibited this pre_requisito from being saved:</h2>

      <ul>
        <% pre_requisito.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
