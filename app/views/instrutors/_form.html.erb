<%= form_with(model: instrutor) do |form| %>
  <% if instrutor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(instrutor.errors.count, "error") %> prohibited this instrutor from being saved:</h2>

      <ul>
        <% instrutor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nome_instrutor, style: "display: block" %>
    <%= form.text_field :nome_instrutor %>
  </div>

  <div>
    <%= form.label :salario, style: "display: block" %>
    <%= form.text_field :salario %>
  </div>

  <div>
    <%= form.label :departamento_id, style: "display: block" %>
    <%= form.text_field :departamento_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
