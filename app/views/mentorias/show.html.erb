<p class="notice"><%= notice %></p>

<div class="aluno-container">
  <%= render @mentorium %>

  <div class="action-buttons">
    <%= link_to "Editar", edit_mentorium_path(@mentorium), class: "btn btn-edit" %>
    <%= link_to "Voltar", mentoria_path, class: "btn btn-back" %>

    <%= button_to "Excluir", @mentorium, method: :delete, class: "btn btn-danger" %>
  </div>
</div>