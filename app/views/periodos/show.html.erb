<p class="notice"><%= notice %></p>

<div class="aluno-container">
  <%= render @periodo %>

  <div class="action-buttons">
    <%= link_to "Editar", edit_periodo_path(@periodo), class: "btn btn-edit" %>
    <%= link_to "Voltar", periodos_path, class: "btn btn-back" %>

    <%= button_to "Excluir", @periodo, method: :delete, class: "btn btn-danger" %>
  </div>
</div>
